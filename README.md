# Java-Homework

lab10-
запуск программ через терминал при помощи

1. mvn clean javafx:run
2. mvn clean javafx:run -P run-widgettoggler
3. mvn clean javafx:run -P run-orderform
4. mvn clean javafx:run -P run-calculator
5. mvn clean javafx:run -P run-textflag
