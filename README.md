# Java-Homework

lab10-
запуск программ через терминал при помощи

1. mvn clean javafx:run
2. mvn clean javafx:run -P run-widgettoggler
3. mvn clean javafx:run -P run-orderform
4. mvn clean javafx:run -P run-calculator
5. mvn clean javafx:run -P run-textflag

lab 12-
Задание 1: mvn javafx:run -P task1-server и mvn javafx:run -P task1-client
Задание 2: mvn javafx:run -P task2-server и mvn javafx:run -P task2-client
Задание 3: mvn javafx:run -P task3-server и дважды mvn javafx:run -P task3-client
